# Frontend build-time config. Copy to `.env`.
# In dev, leave blank — Vite proxies /api to the NestJS backend on :3001.
# In prod, point at the deployed API origin, e.g. https://api.kalqy.app
VITE_API_BASE=
